# Mizuki + Classic 双站使用指南

## 📁 项目结构

```
mizukii/
├── src/                     # Astro (Mizuki) 源代码
│   ├── pages/               # 页面路由
│   ├── content/posts/       # 📝 文章（Markdown）
│   ├── data/                # 数据配置 (skills, timeline, projects)
│   ├── components/          # Astro 组件
│   └── layouts/             # 布局模板
├── public/                  # Astro 静态资源
│   ├── js/                  # JS 脚本 (framework-switch.js, 粒子等)
│   ├── images/              # 全站图片
│   └── classic/             # Classic 站点（构建产物，Git ignore）
├── classic/                 # Hexo (Butterfly) 经典版源代码
│   ├── source/              # 📝 内容源
│   │   ├── _posts/          # 文章（从 Astro 同步）
│   │   ├── _data/           # 数据文件 (link.yml)
│   │   ├── images/          # 图片（从 Astro 同步）
│   │   ├── css/             # 自定义 CSS
│   │   └── js/              # 自定义 JS (particles.js)
│   ├── themes/butterfly/    # Butterfly 主题
│   └── _config.yml          # Classic 站点配置
├── scripts/                 # 🛠 工具脚本
│   ├── sync-all.cjs         # 一键全站同步（推荐）
│   ├── sync-posts.cjs       # 文章同步
│   ├── enhance-classic.cjs  # Classic HTML 增强注入
│   ├── new-post.js          # 新建文章
│   └── fix-scroll.cjs       # 滚动修复
├── dist/                    # Astro 构建输出（Git ignore）
├── astro.config.mjs         # Astro 配置
├── package.json             # 项目依赖与脚本
└── docs/                    # 文档
```

---

## 🚀 快速开始

```bash
# 1. 安装依赖
pnpm install
cd classic && pnpm install && cd ..

# 2. 一键全站同步
pnpm sync-all

# 3. 启动开发服务器
pnpm dev
```

访问：
- **Mizuki 现代版**: http://localhost:4321/
- **Classic 经典版**: http://localhost:4321/classic/

---

## 📝 工作流程

### 日常写文章

```bash
# 新建文章（在 src/content/posts/ 下创建）
pnpm new-post

# 写完后一键同步到两站
pnpm sync-all
```

### 仅同步文章到 Classic

```bash
node scripts/sync-posts.cjs
```

### 仅构建 Classic（不构建 Astro）

```bash
pnpm sync-all --skip-astro
```

### 仅增强注入（Classic HTML 美化）

```bash
node scripts/enhance-classic.cjs
```

---

## 🔄 两站同步机制

| 内容 | Astro (Mizuki) | Classic (Hexo) | 同步方式 |
|------|---------------|----------------|----------|
| **文章** | `src/content/posts/*.md` | `classic/source/_posts/*.md` | `sync-posts.cjs` |
| **图片** | `public/images/` | `classic/source/images/` | `sync-all.cjs` 自动 |
| **友链** | `src/data/friends.json` | `classic/source/_data/link.yml` | 手动更新 |
| **技能** | `src/data/skills.ts` | `classic/source/skills/index.md` | 手动更新 |
| **时间线** | `src/data/timeline.ts` | `classic/source/timeline/index.md` | 手动更新 |
| **公告** | `src/config.ts` | `classic/themes/butterfly/_config.yml` | 手动更新 |
| **头像** | `src/assets/images/avatar.png` | `classic/source/images/avatar.png` | 手动复制 |

---

## 🎨 Classic 站点效果

Classic 站点通过 `enhance-classic.cjs` 脚本自动注入以下效果：

- 🌄 全屏封面背景
- 🌈 白字 + 炫彩流动边框标题
- ✨ 金色漂浮粒子
- 🪟 毛玻璃内容卡片
- 📊 不蒜子访问量统计

---

## ⚙ 导航结构

### Astro 导航 (Others 菜单)
```
Others
├── Projects   → /projects/
├── Skills     → /skills/
├── Timeline   → /timeline/
└── 📜 经典版  → /classic/
```

### Classic 导航 (Others 菜单)
```
Others
├── Projects   → /classic/projects/
├── Skills     → /classic/skills/
├── Timeline   → /classic/timeline/
└── 旧版主题    → 返回 Astro 主站
```

---

## 🧹 常用命令

```bash
# 开发
pnpm dev              # Astro 开发服务器
pnpm start             # 同上

# 构建
pnpm build             # Astro 生产构建
pnpm sync-all           # 全站同步构建

# 部署（GitHub Pages）
pnpm deploy            # 仅部署 Astro
pnpm deploy-all         # 全站部署

# 质量
pnpm check             # Astro 类型检查
pnpm lint              # Biome 代码检查
pnpm format            # Biome 格式化
```

---

## 🔧 兼容性说明

- Classic 站点通过 Astro 的 Vite 中间件代理，开发和生产环境均可正常访问
- 文章 frontmatter 自动转换：`published` → `date`，`category` → `categories`，`image` → `cover`
- 两站使用相同图片资源，修改一次即可同步
- Classic 评论系统已关闭，专注纯内容展示

---

## 📦 注意事项

1. **不要手动编辑** `classic/public/`、`classic/2026/` 等构建产物目录（已被 Git ignore）
2. **修改文章** 在 `src/content/posts/` 中修改，然后运行 `pnpm sync-all`
3. **修改 Classic 配置** 编辑 `classic/_config.yml` 或 `classic/themes/butterfly/_config.yml`
4. **修改 Astro 配置** 编辑 `src/config.ts`
5. `temp_backup/` 目录为临时备份，不会被 Git 追踪
